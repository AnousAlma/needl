PORT=3001
# Firebase Admin — same service account JSON as any server-side Firebase use
# (Project settings → Service accounts → Generate new private key). Do not commit the file.
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccount.json

# Optional: comma-separated origins for CORS (default allows any origin for local dev)
# CORS_ORIGINS=http://localhost:8081
